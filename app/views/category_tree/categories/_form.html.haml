= form.input :code
= form.input :name
= form.input :parent_name, :as => :independent, :required => :true
= form.input :parent_id, :as => :hidden

= form.button :submit
\|
= link_to 'Cancel', cancel_url